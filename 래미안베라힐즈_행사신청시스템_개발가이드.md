# 래미안베라힐즈 행사 신청 시스템 — 개발 가이드

작성일: 2026-08-23

---

## 0. 기획 검토 총평

결론부터 말씀드리면, 이 아이디어는 실제로 만들 가치가 충분합니다. 원데이클래스, 물놀이 행사, 찾아가는 대사증후군 검사처럼 정기적으로 반복되는 입주민 대상 행사가 많은 단지라면, 구글폼·네이버폼 대비 다음 세 가지 차별점이 실질적인 업무 효율로 이어집니다.

첫째, 신청자 본인이 신청 결과와 신청 현황을 언제든 확인할 수 있다는 점입니다. 지금은 관리사무소에 전화해서 "제가 신청이 됐나요?"를 확인하는 경우가 많을 텐데, 이 부담이 사라집니다. 둘째, 신청 내용을 본인이 직접 수정·취소할 수 있어 관리사무소가 일일이 응대하지 않아도 됩니다. 셋째, 관리자 입장에서 행사별 신청현황을 한 화면에서 조회·집계할 수 있어 정원 관리와 명단 작성이 훨씬 수월해집니다.

다만 실제 구현에 들어가기 전에 몇 가지 설계 방향을 미리 정해두는 것이 좋습니다. 이 가이드는 그 설계 방향을 정리하고, 아키텍처·데이터 모델·보안·배포까지 실제 개발에 바로 착수할 수 있는 수준으로 구체화한 문서입니다. 문서 마지막의 "11. 확인이 필요한 사항"은 제가 임의로 판단해 넣은 기본값이므로, 실제 운영 방식과 다르면 알려주시면 됩니다.

### 요청하신 기획에서 특히 잘 짚은 부분
- "동/호수+연락처+비밀번호"만으로 로그인시키는 방식은 회원가입 절차 없이 진입장벽을 낮추면서도, 최소한의 본인확인 수단을 확보한 실용적인 절충안입니다.
- 신청현황에 개인정보를 마스킹해서 노출한다는 아이디어는 정확히 개인정보보호법이 요구하는 "수집목적에 필요한 최소한의 정보만 공개"라는 원칙과 맞아떨어집니다.
- 관리자 메뉴에 "동호수관리"를 별도로 둔 것은 매우 중요한 설계 판단입니다. 이는 곧 "관리사무소가 사전에 등록한 동/호수만 시스템에 진입할 수 있다"는 화이트리스트 방식을 의미하는데, 이렇게 하면 외부인이 아무 동/호수나 입력해서 들어오는 것을 막을 수 있습니다. 아래 아키텍처는 이 방식을 기본 전제로 설계했습니다.

### 미리 정해두면 좋은 부분 (본문에서 구체적으로 다룸)
- 로그인은 매번 "동호수+비밀번호"만 확인하는 방식이라, 표준 Firebase Auth(이메일/비밀번호)를 그대로 쓸 수 없습니다. 별도의 인증 로직(Cloud Functions + Custom Token)이 필요합니다 — 3장 참고.
- 정원이 걸린 행사에 여러 세대가 동시에 신청 버튼을 누르는 상황(동시성)을 처리하지 않으면 정원 초과 신청이 발생할 수 있습니다 — 6장 참고.
- 비밀번호를 잊어버렸을 때의 절차가 필요합니다. 이메일/문자 인증을 붙이지 않는다면 "관리사무소 방문·전화 확인 후 관리자가 초기화"가 현실적인 시작점입니다 — 4장 참고.
- 동/호수, 연락처, 비밀번호를 온라인으로 수집하는 것이라 개인정보보호법상 고지·동의, 안전조치, (Firebase 리전에 따라) 국외이전 이슈를 함께 챙겨야 합니다 — 7장 참고.

---

## 1. 사용자 시나리오 정리

**입주민 흐름**
1. 링크 접속 → 초기화면에서 동/호수/연락처/비밀번호 입력
   - 해당 동/호수가 관리자가 등록해둔 "동호수관리" 목록에 없으면 "등록되지 않은 동/호수입니다. 관리사무소(☎)로 문의해주세요" 안내
   - 목록에는 있지만 아직 비밀번호가 설정되지 않은 최초 접속이면, 입력한 비밀번호로 최초 등록 처리
   - 이미 비밀번호가 설정되어 있으면 일치 여부 확인 후 로그인
2. 로그인 후 행사배너 목록 화면 → 행사명/일시/장소/신청기간/정원이 카드 형태로 노출, 모집중/마감/예정 상태 표시
3. 원하는 배너 클릭 → 신청서 화면. 동/호수·연락처는 자동 입력되어 있고, 참가 희망 일시 등 행사별 추가 항목을 선택 후 "신청하기"
   - 화면 하단에는 마스킹된 신청현황 리스트 표시 (예: "101동 15**호 김*수 010-****-1234")
4. 이후 재접속 시 동/호수+비밀번호로 로그인 → "나의 신청내역"에서 수정/취소 가능

**관리자 흐름**
1. 초기화면의 "관리자 로그인" 버튼 → 이메일/비밀번호로 로그인 (입주민용 로그인과 완전히 분리된 별도 인증)
2. 로그인 성공 시 관리자 전용 메뉴 노출: 행사배너 관리, 신청현황, 동호수관리
3. 행사배너 관리에서 행사 등록/수정/마감 처리, 신청현황에서 행사별 신청자 명단 조회·엑셀 다운로드, 동호수관리에서 유효 동/호수 등록·비밀번호 초기화

---

## 2. 전체 아키텍처

```
[입주민 브라우저]                [관리자 브라우저]
      │                                │
      ▼                                ▼
        React(Vite) SPA — Firebase Hosting
                      │
        ┌─────────────┴─────────────┐
        ▼                           ▼
  Cloud Functions              Cloud Firestore
  (로그인/신청/현황조회 등        (households, events,
   서버 로직 전담)                applications, admins)
        │
        ▼
  Firebase Authentication
  (Custom Token: 세대,  이메일/비밀번호: 관리자)
```

- **프론트엔드**: React + Vite (또는 Next.js) 싱글 페이지 앱, Firebase Hosting에 정적 배포
- **서버 로직**: Cloud Functions(Node.js/TypeScript) — 세대 로그인/등록, 신청 접수(트랜잭션), 마스킹된 신청현황 조회, 관리자 엑셀 내보내기처럼 "신뢰할 수 있는 서버에서 처리해야 하는 로직"을 담당
- **데이터베이스**: Cloud Firestore, **리전은 반드시 `asia-northeast3`(서울)로 지정** — 7장의 개인정보 국외이전 이슈와 직결
- **인증**: 입주민은 Firebase Custom Token 방식(아래 4장), 관리자는 표준 Firebase Auth 이메일/비밀번호 + Custom Claim(`admin: true`)
- **배포**: GitHub 저장소 → GitHub Actions → `firebase deploy` (Hosting + Functions)

---

## 3. Firestore 데이터 모델

| 컬렉션 | 문서 ID | 주요 필드 | 설명 |
|---|---|---|---|
| `households` | `{동}-{호}` 예: `101-1502` | `dong, ho, phone, passwordHash, isRegistered, residentAlias, failedAttempts, lockUntil, createdAt, updatedAt` | 관리자가 사전 등록하는 유효 동/호수 화이트리스트 겸 로그인 정보 |
| `events` | 자동 ID | `title, description, place, applyStart, applyEnd, eventStart, eventEnd, capacity, appliedCount, status(draft/open/closed), bannerImageUrl, extraFields, createdBy, createdAt` | 행사 배너 정보. `extraFields`에 "참가 희망 일시" 같은 행사별 커스텀 선택항목을 배열로 정의 |
| `applications` | 자동 ID | `eventId, householdId, dong, ho, phone, selectedOption, status(applied/cancelled), appliedAt, cancelledAt` | 신청 내역. `householdId`가 곧 로그인 세대의 인증 식별자와 동일하게 설계해 보안규칙과 연결 |
| `admins` | Firebase Auth `uid` | `email, name, role(super/staff), createdAt` | 관리자 계정 |

`households`의 문서 ID를 "동-호" 자체로 쓰면, 나중에 Firebase Auth의 `uid`도 동일하게 맞출 수 있어 보안규칙 작성이 단순해집니다(4장 참고).

---

## 4. 인증/보안 설계 (가장 중요한 부분)

일반적인 Firebase Auth는 이메일/비밀번호나 전화번호 인증을 전제로 하지만, 이 시스템은 "동/호수+비밀번호"라는 자체 규칙으로 로그인해야 합니다. 이럴 때 권장되는 정석적인 패턴이 **Custom Token 인증**입니다.

**동작 방식**
1. 클라이언트가 동/호수/연락처/비밀번호를 Cloud Function(`householdLogin`)으로 전송
2. Function이 `households/{동}-{호}` 문서를 조회
   - 문서가 없으면 → 화이트리스트에 없는 동/호수 → 오류 반환("관리사무소 문의")
   - `passwordHash`가 비어있으면 → 최초 접속 → 입력한 비밀번호를 해시(bcrypt)해서 저장(최초 등록)
   - `passwordHash`가 있으면 → 입력값과 비교해 불일치 시 오류 반환
3. 인증 성공 시 Firebase Admin SDK로 `createCustomToken("{동}-{호}")` 발급
4. 클라이언트는 받은 토큰으로 `signInWithCustomToken()` 호출 → 이후 Firestore 보안규칙에서 `request.auth.uid`가 곧 세대 식별자가 됨

```js
// functions/index.js (개념 예시)
exports.householdLogin = onCall(async (req) => {
  const { dong, ho, phone, password } = req.data;
  const id = `${dong}-${ho}`;
  const ref = db.doc(`households/${id}`);
  const snap = await ref.get();

  if (!snap.exists) {
    throw new HttpsError('not-found', '등록되지 않은 동/호수입니다. 관리사무소로 문의해주세요.');
  }
  const hh = snap.data();

  if (hh.lockUntil && hh.lockUntil.toMillis() > Date.now()) {
    throw new HttpsError('resource-exhausted', '비밀번호 오류 누적으로 잠시 후 다시 시도해주세요.');
  }

  if (!hh.passwordHash) {
    const hash = await bcrypt.hash(password, 10);
    await ref.update({ passwordHash: hash, phone, isRegistered: true, updatedAt: FieldValue.serverTimestamp() });
  } else {
    const ok = await bcrypt.compare(password, hh.passwordHash);
    if (!ok) {
      await ref.update({ failedAttempts: FieldValue.increment(1) });
      throw new HttpsError('permission-denied', '비밀번호가 일치하지 않습니다.');
    }
    await ref.update({ failedAttempts: 0, phone, updatedAt: FieldValue.serverTimestamp() });
  }

  const token = await admin.auth().createCustomToken(id, { dong, ho });
  return { token };
});
```

**Firestore 보안규칙 (개념 예시)**

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    match /households/{id} {
      allow read, write: if request.auth != null && request.auth.uid == id;
      allow read, write: if request.auth.token.admin == true;
    }

    match /applications/{id} {
      allow create: if request.auth != null
                     && request.resource.data.householdId == request.auth.uid;
      allow read, update: if request.auth != null
                     && resource.data.householdId == request.auth.uid;
      allow read, write: if request.auth.token.admin == true;
    }

    match /events/{id} {
      allow read: if request.auth != null;
      allow write: if request.auth.token.admin == true;
    }
  }
}
```

이렇게 하면 클라이언트는 본인 세대의 신청 내역만 직접 읽고 쓸 수 있고, 다른 세대의 신청현황(마스킹된 목록)은 Firestore를 직접 조회하는 것이 아니라 아래처럼 **서버(Cloud Function)가 마스킹 처리한 결과만 내려주는 방식**으로 분리하는 것을 권장합니다.

```js
// 신청현황 조회는 반드시 서버에서 마스킹 후 반환
exports.getApplicationStatus = onCall(async (req) => {
  const { eventId } = req.data;
  const snaps = await db.collection('applications')
    .where('eventId', '==', eventId)
    .where('status', '==', 'applied')
    .get();

  return snaps.docs.map(d => {
    const a = d.data();
    return {
      dong: a.dong,
      ho: maskHo(a.ho),                 // 15**호
      name: maskName(a.residentAlias),  // 김*수 / 김철
      phoneTail: a.phone.slice(-4),     // 끝 4자리만
    };
  });
});
```

이렇게 서버에서 마스킹 로직(이름 3글자면 가운데 삭제, 2글자면 성만 노출 등)을 일괄 처리하면, 클라이언트 코드나 보안규칙에서 개인정보가 새어나갈 여지가 없습니다.

**비밀번호 분실 시**: 이메일이나 문자 인증 수단이 없는 초기 단계에서는 "관리사무소 방문 또는 전화로 본인(동/호수) 확인 → 관리자가 동호수관리 화면에서 해당 세대의 `passwordHash`를 초기화 → 다음 로그인 시 새 비밀번호로 재등록"이 가장 현실적입니다. 이후 예산이 허용되면 문자(SMS) 인증을 추가하는 것을 2단계 과제로 남겨두시면 됩니다.

**무차별 대입 공격 방지**: 위 코드처럼 `failedAttempts`/`lockUntil` 필드로 일정 횟수 실패 시 일시 잠금을 걸고, 가능하면 Firebase App Check을 함께 적용하는 것을 권장합니다.

---

## 5. 화면 구성

| 화면 | 대상 | 주요 내용 |
|---|---|---|
| 초기화면(로그인/최초등록) | 입주민 | 동/호수/연락처/비밀번호 입력, "관리자 로그인" 버튼 |
| 행사배너 목록 | 입주민 | 카드형 목록 (행사명/일시/장소/신청기간/정원/모집상태) |
| 행사 상세·신청서 | 입주민 | 행사 정보 재확인, 동/호수·연락처 자동입력, 희망 일시 등 선택, 개인정보 수집동의, 신청하기, 하단 마스킹 신청현황 |
| 나의 신청내역 | 입주민 | 재로그인 후 본인 신청건 목록, 수정/취소 |
| 관리자 로그인 | 관리자 | 이메일/비밀번호 |
| 행사배너 관리 | 관리자 | 행사 등록/수정/마감, 배너 이미지 업로드 |
| 신청현황 | 관리자 | 행사별 신청자 전체 명단(비마스킹), 엑셀 다운로드 |
| 동호수관리 | 관리자 | 유효 동/호수 등록·일괄 업로드(CSV), 비밀번호 초기화, 세대 삭제(이사 처리) |

---

## 6. 정원(동시성) 처리

여러 세대가 거의 동시에 마지막 남은 자리에 신청하면, 단순히 "신청자 수를 세고 정원과 비교"하는 방식은 두 명 모두 통과해버리는 경쟁 조건(race condition)이 생길 수 있습니다. Firestore의 `runTransaction`으로 "정원 확인 → 카운트 증가 → 신청 문서 생성"을 하나의 원자적 작업으로 묶어야 합니다.

```js
await db.runTransaction(async (tx) => {
  const eventRef = db.doc(`events/${eventId}`);
  const eventSnap = await tx.get(eventRef);
  const event = eventSnap.data();

  if (event.appliedCount >= event.capacity) {
    throw new HttpsError('resource-exhausted', '정원이 마감되었습니다.');
  }

  tx.update(eventRef, { appliedCount: FieldValue.increment(1) });
  tx.set(db.doc(`applications/${newId}`), { ...applicationData });
});
```

---

## 7. 개인정보 보호 — 법적 근거와 실무 반영

동/호수, 연락처, 비밀번호를 온라인으로 수집·저장하는 시스템이므로 개인정보보호법상 아래 세 가지는 실무적으로 반드시 챙기시는 것을 권합니다. (법령 확인: 개인정보 보호법, 2025-04-01 개정, 2025-10-02 시행 기준)

**① 수집·이용 동의 (제15조)**
개인정보처리자는 정보주체의 동의를 받거나 계약 이행에 불가피한 경우 등에만 개인정보를 수집·이용할 수 있고, 동의를 받을 때는 수집목적, 수집 항목, 보유·이용 기간, 동의 거부권 및 불이익 내용을 미리 알려야 합니다. 실무적으로는 초기화면(최초 등록 시) 또는 신청서 하단에 체크박스 형태로 "개인정보 수집·이용에 동의합니다 (수집항목: 동/호수, 연락처 / 목적: 행사 신청·운영 / 보유기간: 탈퇴·행사 종료 후 O년)"를 넣고, 별도 페이지로 개인정보처리방침을 게시하시는 것을 권장합니다.

**② 안전조치의무 (제29조)**
개인정보가 분실·유출되지 않도록 기술적·관리적 조치를 취할 의무입니다. 실무 반영: 비밀번호는 절대 평문 저장 금지(위 코드처럼 bcrypt 해시 저장), Cloud Functions 접근 로그 보관, 관리자 계정에도 강력한 비밀번호와 가능하면 2단계 인증 적용을 권장합니다.

**③ 국외 이전 (제28조의8)**
개인정보처리자는 원칙적으로 개인정보를 국외로 이전(제공·위탁·보관)할 수 없고, 별도 동의를 받거나 처리방침에 국외이전 사항을 공개하는 등 예외 요건을 갖춰야만 가능합니다. Firebase(Google Cloud) 프로젝트를 만들 때 리전을 기본값(미국 등)으로 두면 이 조항에 걸릴 수 있으므로, **Firestore/Cloud Functions/Storage 생성 시 리전을 `asia-northeast3`(서울)로 명시적으로 선택**하시면 국내 저장이 되어 국외이전 고지·동의 부담을 피할 수 있습니다. 이 부분은 개발 초기, 프로젝트 생성 단계에서 반드시 확인해야 하는 항목입니다.

이 외에 세대가 이사한 경우 기존 세대 정보(동/호수, 비밀번호)를 그대로 남겨두면 새 입주민이 이전 거주자의 로그인을 넘겨받는 문제가 생기므로, "동호수관리"에 이사 처리(계정 초기화) 기능을 넣어 운영 프로세스로 관리하시는 것을 권장합니다.

---

## 8. GitHub + Firebase 배포

**저장소 구조 (예시)**
```
event-application-system/
├─ web/                # React(Vite) 프론트엔드
├─ functions/          # Cloud Functions (Node.js/TypeScript)
├─ firestore.rules
├─ firestore.indexes.json
├─ firebase.json
└─ .github/workflows/deploy.yml
```

**절차**
1. `firebase init` 으로 Hosting, Functions, Firestore 설정 (리전은 asia-northeast3)
2. 프론트엔드 환경변수(Firebase 설정값)는 `web/.env`에 `VITE_` 접두사로 관리하고 `.gitignore`에 포함
3. GitHub 저장소에 소스 업로드, `main` 브랜치를 운영 배포 기준으로 사용
4. GitHub Actions 워크플로우에서 `main` 브랜치 push 시 자동 빌드·배포

```yaml
# .github/workflows/deploy.yml (개념 예시)
name: Deploy to Firebase
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20 }
      - run: npm ci --prefix web && npm run build --prefix web
      - run: npm ci --prefix functions
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          projectId: your-firebase-project-id
      - run: npx firebase deploy --only functions,firestore:rules --token ${{ secrets.FIREBASE_TOKEN }}
```

배포 계정 정보(서비스 계정 JSON, 토큰)는 반드시 GitHub Secrets에 저장하고 소스코드에 커밋하지 않아야 합니다.

---

## 9. 비용

Cloud Functions를 사용하려면 Firebase 요금제를 무료(Spark)에서 종량제(Blaze)로 전환해야 합니다. 다만 Blaze 요금제도 매달 일정량(예: 함수 호출 200만 건, Firestore 읽기/쓰기 5만~2만 건 등)까지 무료 할당량이 있어서, 한 단지 규모(수백 세대, 월 수천 건 수준의 신청·조회)에서는 실제 과금이 거의 발생하지 않습니다. 다만 예상치 못한 과금을 막기 위해 Firebase 콘솔에서 예산 알림(Budget Alert)을 설정해두시는 것을 권장합니다.

---

## 10. 단계별 개발 로드맵 (제안)

**1단계 (MVP)**: 초기 로그인/최초등록, 행사배너 목록 및 관리자 CRUD, 신청서 작성·제출(정원 트랜잭션 포함), 마스킹된 신청현황 조회, 나의 신청내역 수정·취소, 관리자 신청현황 조회 및 엑셀 다운로드, 동호수관리(등록/초기화)

**2단계**: 신청 완료 시 문자(SMS) 또는 카카오 알림톡 발송 연동, 정원 마감 시 대기신청 기능, 관리자 대시보드(행사별 통계)

**3단계**: 관리자 다중 계정·권한 분리(super/staff), 반복 행사 템플릿 기능, 참가 후 만족도 설문 연동

1단계만으로도 기존 방문 신청 대비 충분한 개선 효과가 있으므로, 1단계를 먼저 완성해 실제 행사(예: 다음 원데이클래스)에 시범 적용해보고 피드백을 반영해 2단계로 넘어가는 방식을 권장합니다.

---

## 11. 확인이 필요한 사항 (제가 임의로 기본값을 정한 부분)

아래는 문서 작성을 위해 합리적이라고 판단해 기본값을 정한 부분입니다. 실제 운영 방침과 다르면 말씀해주시면 반영하겠습니다.

- **세대당 중복 신청**: 기본값은 "1세대 1행사 1신청"으로 제한(이미 신청한 세대는 재신청 시 안내 후 차단). 가족 여러 명이 각각 신청해야 하는 행사(예: 원데이클래스 정원 관리)라면 신청서에 "참가 인원수" 필드를 추가하는 방식을 권장합니다.
- **비밀번호 분실 처리**: 기본값은 "관리사무소 방문·전화 확인 후 관리자가 초기화". SMS 인증 도입은 비용이 발생하므로 2단계 과제로 분류했습니다.
- **동호수 화이트리스트 등록 범위**: 전체 세대를 한 번에 일괄 등록(CSV 업로드)할지, 신청 들어올 때마다 개별 확인 후 등록할지는 초기 데이터 확보 방법에 따라 달라집니다. 전체 세대 명단이 있다면 일괄 업로드 기능을 1단계에 포함하는 것을 권장합니다.

---

## 요약

기획 자체는 실제 관리사무소 업무 부담을 줄이는 명확한 니즈에 기반하고 있어 진행할 가치가 충분합니다. 핵심은 "동/호수+비밀번호"라는 단순한 인증 방식을 안전하게 구현하는 것(Custom Token + 서버 측 마스킹)과, 개인정보 수집에 따르는 법적 의무(동의 고지, 비밀번호 해시 저장, Firestore 리전을 서울로 설정)를 초기 설계 단계에서부터 반영하는 것입니다. 이 문서의 구조를 그대로 GitHub 저장소의 설계 문서로 사용하시면 실제 개발 착수 시 바로 참고할 수 있습니다.
